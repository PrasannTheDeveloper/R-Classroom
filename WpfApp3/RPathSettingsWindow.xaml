<Window x:Class="WpfApp3.RPathSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="R Executable Path Settings" Height="300" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Configure R Installation Path" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Text="Specify the path to your R executable (Rscript.exe or R.exe):" 
                   Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TextBox x:Name="PathTextBox" Grid.Row="2" Margin="0,0,0,10" Height="25" 
                 FontFamily="Consolas" Padding="5"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Browse..." Width="100" Height="30" Margin="0,0,10,0" Click="BrowseButton_Click"/>
            <Button Content="Test Path" Width="100" Height="30" Margin="0,0,10,0" Click="TestButton_Click"/>
            <Button Content="OK" Width="80" Height="30" Margin="0,0,10,0" IsDefault="True" Click="OKButton_Click"/>
            <Button Content="Cancel" Width="80" Height="30" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="R not found? Download it from the official R website:" 
                   Margin="0,15,0,5" FontWeight="SemiBold" Foreground="#DC3545"/>

        <Button Grid.Row="5" Content="Download R for Windows" Width="200" Height="30" 
                HorizontalAlignment="Left" Background="#28A745" Foreground="White" 
                BorderThickness="0" Click="DownloadButton_Click"/>
    </Grid>
</Window>